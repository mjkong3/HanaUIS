<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="ateam.mapper.ProfessorMapper">

	<select id="selectProfessor" parameterType="int" resultType="ateam.dto.ProfessorDTO">
		SELECT professor_id, name, department_code FROM PROFESSOR WHERE PROFESSOR_ID = #{professorId}
	</select>
	
	<!-- 교수 로그인처리 -->
	<select id="login_check" resultType="ateam.dto.ProfessorDTO">
		SELECT PROFESSOR_ID, 
				EMAIL,
				NAME,
				DEPARTMENT_CODE
		FROM PROFESSOR
		WHERE PROFESSOR_ID = #{professorId}
		AND PASSWORD = #{password}
	</select>

</mapper>