<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="ateam.mapper.BoardMapper">

<!-- 공지사항 페이징 처리 -->
<select id="getBoardCount" resultType="int">
    SELECT COUNT(*) FROM BOARD;
</select>
<select id="boardList" parameterType="map" resultType="ateam.dto.BoardDTO">
   SELECT BOARD_CODE, TITLE, CONTENT
   FROM BOARD 
   ORDER BY BOARD_CODE DESC
   LIMIT #{pageSize} OFFSET #{offset};
</select>

<!-- 공지사항 상세 -->
<select id="selectBoard" parameterType="Integer" resultType="ateam.dto.BoardDTO">
   SELECT  BOARD_CODE,  
         TITLE, 
         CONTENT, 
      
         FILE_CODE 
   FROM BOARD
   WHERE BOARD_CODE = #{id}
</select>

<!-- 공지사항 파일 -->
<select id="selectFile" parameterType="String" resultType="ateam.dto.FileDTO">
   SELECT FILE_NAME
   FROM FILE
   WHERE FILE_CODE = #{fileCode}
</select>

</mapper>