<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="ateam.mapper.BoardMapper">

<!-- 공지사항 -->
<select id="boardList" resultType="ateam.dto.BoardDTO">
	SELECT BOARD_CODE, TITLE, CONTENT, BOARD_DATE FROM BOARD ORDER BY BOARD_CODE DESC;
</select>

<!-- 공지사항 상세 -->
<select id="selectBoard" parameterType="Integer" resultType="ateam.dto.BoardDTO">
	SELECT BOARD_CODE, TITLE, CONTENT, BOARD_DATE FROM BOARD WHERE BOARD_CODE = #{id} ORDER BY BOARD_CODE DESC;
</select>

</mapper>